data:
  manifest: data/manifests/paired_real_train.csv
  patch_size: [64, 128, 128]
  target_spacing: [1.5, 1.0, 1.0]
  artifact_probability: 0.0
  num_workers: 8
model:
  in_channels: 3
  base_channels: 24
  levels: 4
  blocks_per_level: 2
train:
  stage: paired_real_adaptation
  epochs: 80
  batch_size: 1
  learning_rate: 0.00001
  weight_decay: 0.0001
  grad_accumulation: 4
  amp: true
  ema_decay: 0.9995
  pretrained: checkpoints/finetune/best.pt
  checkpoint_dir: checkpoints/paired_real
loss:
  artifact_l1: 10.0
  global_l1: 1.0
  gradient: 1.0
  ssim: 0.5
  identity: 4.0
  uncertainty: 0.2
hardware:
  device: auto
  precision: auto
  auto_tune: false
  allow_tf32: true
  cudnn_benchmark: true
  channels_last_3d: true
  compile: false
  prefetch_factor: 2
